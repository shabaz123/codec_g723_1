name: codec_g723_1
description: High-performance G.723.1 speech codec for Flutter and Dart. Supports 6.3k/5.3k bitrates, 8 kHz & 16 kHz audio, streaming chunked I/O, and background isolates.
version: 0.1.0
repository: https://github.com/shabaz123/codec_g723_1
issue_tracker: https://github.com/shabaz123/codec_g723_1/issues

topics:
  - audio
  - codec
  - g723
  - voip
  - ffi

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.12.2

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.2.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
